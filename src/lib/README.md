# Application libraries

This directory contains reusable application logic, API clients, adapters, formatters, and domain helpers that are shared by routes, features, and components.

Keep route handlers and pages in `src/app`. Shared domain code should live here so non-route modules do not depend on the App Router tree.
