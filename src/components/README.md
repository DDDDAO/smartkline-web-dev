# Shared components

This directory contains reusable UI primitives, chart widgets, providers, and analytics adapters that are not owned by a single route or product workflow.

Do not put feature orchestration, workspace state, or product-specific panels here. Those belong under `src/features/*` and should be exported through a small feature entry point.
