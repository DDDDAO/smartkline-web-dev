# Shared domain types

This directory contains domain-level TypeScript types shared across routes, feature modules, reusable components, and application libraries.

Keep feature-private prop types close to their feature. Promote a type here only when it is reused across module boundaries.
